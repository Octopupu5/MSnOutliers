#ifndef DEFAULTS_H
#define DEFAULTS_H

namespace CP {
    namespace UI {
        enum class Defaults: int {
            DIALOG_WIDTH = 300,
            DIALOG_HEIGHT = 100,
            MAIN_WINDOW_WIDTH = 1700,
            MAIN_WINDOW_HEIGHT = 300,
            FEATURES = 3,
            SAMPLES = 100,
            GEN_MAX = 100,
            GEN_MIN = 1,
            GEN_DISPLAY = 25,
            IMAGES_WIDTH = 670,
            IMAGES_HEIGHT = 490
        };
    } // namespace UI;
} // namespace CP;

#endif